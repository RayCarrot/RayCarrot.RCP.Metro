<UserControl x:Class="RayCarrot.RCP.Metro.Utility_RaymanOrigins_HQVideos_UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Interactions="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
             ResxExtension.DefaultResxName="{x:Static local:LocalizationManager.ResourcePath}"
             mc:Ignorable="d"
             d:DesignWidth="550" d:DataContext="{d:DesignInstance local:Utility_RaymanOrigins_HQVideos_ViewModel}">
    <StackPanel>
        <Interactions:Interaction.Behaviors>
            <local:UnifiedMarginBehavior Margin="{StaticResource Thickness.Vertical}" />
        </Interactions:Interaction.Behaviors>

        <Button Content="{Resx ROU_HQVideosReplace}"
                HorizontalAlignment="Left"
                Command="{Binding Path=ReplaceVideosCommand}"
                IsEnabled="{Binding Path=CanVideosBeReplaced}"
                Visibility="{Binding Path=IsOriginalVideos, Converter={local:BooleanToVisibilityConverter}}"/>

        <Button Content="{Resx ROU_HQVideosRevert}" 
                HorizontalAlignment="Left"
                Command="{Binding Path=ReplaceVideosCommand}"
                IsEnabled="{Binding Path=CanVideosBeReplaced}"
                Visibility="{Binding Path=IsOriginalVideos, Converter={local:InvertedBooleanToVisibilityConverter}}"/>

    </StackPanel>
</UserControl>