<UserControl x:Class="RayCarrot.RCP.Metro.GameProgression_UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:GameProgression_BaseViewModel}">
    <ItemsControl ItemsSource="{Binding Path=ProgressionSlots, Mode=OneTime}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                    <Separator x:Name="ItemSeparator"
                               Margin="0 7" />

                    <Grid>
                        <TextBlock Text="{Binding Path=SlotName.Value}"
                                   Style="{StaticResource TextBlock.Header}"
                                   Margin="{StaticResource Thickness.VerticalSmall}" />

                        <Grid HorizontalAlignment="Right"
                              Visibility="{Binding Path=CanModify, Converter={local:BooleanToVisibilityConverter}, Mode=OneTime}">
                            <local:OverflowMenu local:UserLevelAssist.MinUserLevel="Technical"
                                                Margin="7">
                                <local:OverflowMenu.ContextMenu>
                                    <ContextMenu ResxExtension.DefaultResxName="{x:Static local:LocalizationManager.ResourcePath}">
                                        <local:MenuItemIcon Header="{Resx Progression_Export}"
                                                            Command="{Binding Path=ExportCommand, Mode=OneTime}"
                                                            IconKind="FileExportOutline" />
                                        <local:MenuItemIcon Header="{Resx Progression_Import}"
                                                            Command="{Binding Path=ImportCommand, Mode=OneTime}"
                                                            IconKind="FileImportOutline" />
                                    </ContextMenu>
                                </local:OverflowMenu.ContextMenu>
                            </local:OverflowMenu>
                        </Grid>

                    </Grid>

                    <ItemsControl ItemsSource="{Binding Path=Items, Mode=OneTime}">
                        <ItemsControl.ItemContainerStyle>
                            <Style>
                                <Setter Property="Control.ToolTip" Value="{Binding Path=Description.Value}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid TextBlock.FontSize="16"
                                      Margin="0 0 0 4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0"
                                           Source="{Binding Path=IconImageSource, Mode=OneTime}"
                                           Margin="0 0 8 0" />

                                    <ContentPresenter Grid.Column="1"
                                                      Content="{Binding Path=Content.Value}"
                                                      VerticalAlignment="Center" />

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource PreviousData}}" Value="{x:Null}">
                        <Setter Property="Visibility" TargetName="ItemSeparator" Value="Collapsed" />
                    </DataTrigger>
                </DataTemplate.Triggers>

            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>