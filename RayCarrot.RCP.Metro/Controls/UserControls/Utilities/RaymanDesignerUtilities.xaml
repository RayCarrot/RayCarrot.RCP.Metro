<rwpf:VMUserControl x:Class="RayCarrot.RCP.Metro.RaymanDesignerUtilities"
                    x:TypeArguments="local:RaymanDesignerUtilitiesViewModel"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
                    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                    xmlns:rwpf="clr-namespace:RayCarrot.WPF;assembly=RayCarrot.WPF"
                    ResxExtension.DefaultResxName="{x:Static local:AppLanguages.ResourcePath}"
                    mc:Ignorable="d" 
                    d:DesignHeight="450" d:DesignWidth="800"
                    d:DataContext="{d:DesignInstance local:RaymanDesignerUtilitiesViewModel}">
    <ScrollViewer x:Name="scrollViewer"
                  Padding="5">
        <StackPanel>
            <i:Interaction.Behaviors>
                <rwpf:UnifiedMarginBehavior Margin="0 8" />
            </i:Interaction.Behaviors>

            <StackPanel Orientation="Horizontal">

                <TextBlock Style="{StaticResource HeaderTextBlock}"
                           Text="{Resx RDU_ReplaceFilesHeader}" />

                <local:InfoIcon Style="{StaticResource AdditionalFilesRequired}" />

                <local:InfoIcon Text="{Resx RDU_ReplaceFilesInfo}" />

            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" MinWidth="110" />
                </Grid.ColumnDefinitions>

                <Grid.Resources>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Padding" Value="0 0 10 0" />
                    </Style>
                </Grid.Resources>

                <i:Interaction.Behaviors>
                    <rwpf:UnifiedMarginBehavior Margin="0 8" />
                </i:Interaction.Behaviors>

                <TextBlock Grid.Column="0"
                           Text="{Resx RDU_ReplaceFilesLanguage}"/>

                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding Path=MapperLanguage, Mode=OneTime, Converter={rwpf:EnumToListConverter}}"
                          SelectedIndex="{Binding Path=MapperLanguage, Converter={rwpf:EnumToIntConverter}}" />

            </Grid>

            <Button Content="{Resx RDU_ReplaceFilesReplace}"
                    Command="{Binding Path=ReplaceRayKitCommand}"
                    HorizontalAlignment="Left"/>

            <Separator />

            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource HeaderTextBlock}"
                           Text="{Resx RDU_CreateConfigHeader}" />

                <local:InfoIcon Text="{Resx RDU_CreateConfigInfo}" />

            </StackPanel>

            <Button Content="{Resx RDU_CreateConfigCreate}"
                    Command="{Binding Path=CreateConfigCommand}"
                    HorizontalAlignment="Left"/>

        </StackPanel>
    </ScrollViewer>
</rwpf:VMUserControl>