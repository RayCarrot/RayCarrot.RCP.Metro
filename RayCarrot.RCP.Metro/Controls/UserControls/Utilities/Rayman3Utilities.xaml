<rwpf:VMUserControl x:Class="RayCarrot.RCP.Metro.Rayman3Utilities"
                    x:TypeArguments="local:Rayman3UtilitiesViewModel"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
                    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                    xmlns:rwpf="clr-namespace:RayCarrot.WPF;assembly=RayCarrot.WPF"
                    ResxExtension.DefaultResxName="{x:Static local:AppLanguages.ResourcePath}"
                    mc:Ignorable="d" 
                    d:DesignHeight="450" d:DesignWidth="800"
                    d:DataContext="{d:DesignInstance local:Rayman3UtilitiesViewModel}">
    <ScrollViewer x:Name="scrollViewer"
                  Padding="5">
        <StackPanel>
            <i:Interaction.Behaviors>
                <rwpf:UnifiedMarginBehavior Margin="0 8" />
            </i:Interaction.Behaviors>

            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource HeaderTextBlock}"
                           Text="DirectPlay" />

                <local:InfoIcon Text="DirectPlay needs to be enabled in order to run Rayman 3. By default it will usually be disabled on Windows 10." />

            </StackPanel>

            <Button Content="Enable DirectPlay"
                    HorizontalAlignment="Left" 
                    Command="{Binding Path=EnableDirectPlayCommand}"/>

            <Button Content="Disable DirectPlay"
                    HorizontalAlignment="Left" 
                    Command="{Binding Path=DisableDirectPlayCommand}"/>

        </StackPanel>
    </ScrollViewer>
</rwpf:VMUserControl>