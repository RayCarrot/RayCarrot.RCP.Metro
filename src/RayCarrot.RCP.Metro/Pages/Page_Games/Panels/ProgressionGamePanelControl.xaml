<UserControl x:Class="RayCarrot.RCP.Metro.ProgressionGamePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignWidth="200" d:DesignHeight="135"
             d:DataContext="{d:DesignInstance local:ProgressionGamePanelViewModel}"
             d:Background="{DynamicResource RCP.Brushes.Card.SecondaryBackground}"
             d:Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}">
    <!-- TODO-UPDATE: Localize -->
    <Grid DataContext="{Binding Path=PrimarySlot}" 
          Visibility="{Binding Converter={local:InvertedObjectNullToVisibilityConverter}}"
          VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Progress data items -->
        <ItemsControl Grid.Column="0" 
                      ItemsSource="{Binding Path=PrimaryDataItems, Mode=OneTime}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0 4">
                        <Image Source="{Binding Path=IconImageSource, Mode=OneTime}"
                               VerticalAlignment="Center"
                               Width="24" Height="24"
                               Margin="0 0 8 0" />

                        <TextBlock Text="{Binding Path=Text.Value}"
                                   FontSize="14"
                                   FontFamily="{StaticResource RCP.Fonts.RaymanOrigins}"
                                   VerticalAlignment="Center"
                                   Margin="0 0 15 0" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Percentage -->
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal"
                        TextElement.FontSize="22"
                        TextElement.FontFamily="{StaticResource RCP.Fonts.RaymanOrigins}"
                        HorizontalAlignment="Center"
                        Margin="0 0 0 8">

                <TextBlock Text="{Binding Path=Percentage, StringFormat=0.##, Mode=OneTime}" />
                <TextBlock Text=" %" />

            </StackPanel>

            <mah:MetroProgressBar Foreground="{Binding Path=ProgressBrush, Mode=OneTime}"
                                  Minimum="0"
                                  Maximum="100"
                                  Width="80"
                                  Value="{Binding Path=Percentage, Mode=OneTime}" />

        </StackPanel>

    </Grid>
</UserControl>