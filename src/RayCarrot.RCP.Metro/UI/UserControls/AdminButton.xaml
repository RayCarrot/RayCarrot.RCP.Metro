<UserControl x:Class="RayCarrot.RCP.Metro.AdminButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RayCarrot.RCP.Metro"
             ResxExtension.DefaultResxName="{x:Static local:LocalizationManager.ResourcePath}">
    <Button Style="{StaticResource RCP.Styles.Button.Flat}"
            ToolTip="{Resx Settings_AdminInfo}"
            Command="{Binding Path=RequestRestartAsAdminCommand, Source={x:Static local:Services.App}}"
            local:ButtonAssist.IconKind="Security"
            local:ButtonAssist.IconSize="16" />
</UserControl>