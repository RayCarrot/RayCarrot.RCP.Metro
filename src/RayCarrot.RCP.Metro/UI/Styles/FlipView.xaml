<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <!-- Default FlipView -->

    <Style TargetType="{x:Type metro:FlipView}" BasedOn="{StaticResource {x:Type metro:FlipView}}">
        <Setter Property="ButtonForwardContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <iconPacks:PackIconMaterial Kind="ArrowRightDropCircleOutline"
                                                Width="24"
                                                Height="24"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ButtonBackContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <iconPacks:PackIconMaterial Kind="ArrowLeftDropCircleOutline"
                                                Width="24"
                                                Height="24"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <!-- TODO-UPDATE: Re-style this -->
        <Setter Property="IndexItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="Margin" Value="8 8 8 0" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Ellipse x:Name="Bd"
                                         Width="{DynamicResource FlipViewIndexItemSize}"
                                         Height="{DynamicResource FlipViewIndexItemSize}"
                                         Margin="{TemplateBinding Padding}"
                                         Fill="{DynamicResource MahApps.Brushes.SystemControlTransparent}"
                                         SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                         Stroke="{DynamicResource MahApps.Brushes.SystemControlForegroundBaseMedium}"
                                         StrokeThickness="1" />
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="true">
                                        <Setter TargetName="Bd" Property="Fill" Value="{DynamicResource MahApps.Brushes.SystemControlHighlightListAccentMedium}" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Cursor" Value="Hand" />
                                        <Setter TargetName="Bd" Property="Fill" Value="{DynamicResource MahApps.Brushes.SystemControlHighlightListAccentHigh}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>