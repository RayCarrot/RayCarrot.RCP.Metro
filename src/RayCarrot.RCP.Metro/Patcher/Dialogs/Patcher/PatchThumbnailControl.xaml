<UserControl x:Class="RayCarrot.RCP.Metro.Patcher.Dialogs.Patcher.PatchThumbnailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:rcp="clr-namespace:RayCarrot.RCP.Metro"
             xmlns:patcher="clr-namespace:RayCarrot.RCP.Metro.Patcher.Dialogs.Patcher"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance patcher:InstalledPatchViewModel}"
             ResxExtension.DefaultResxName="{x:Static rcp:LocalizationManager.ResourcePath}">
    <Grid>

        <Image Source="{Binding Path=Thumbnail}"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"/>

        <iconPacks:PackIconMaterial Visibility="{Binding Path=Thumbnail, Converter={rcp:ObjectNullToVisibilityConverter}}" 
                                    Kind="FileSettingsOutline" 
                                    Width="Auto" Height="Auto"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center" />

    </Grid>
</UserControl>